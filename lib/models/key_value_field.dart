/// A generic label/value row — used for the Admin vendor/driver detail
/// field lists and each admin order card's 2x2 field grid.
class KeyValueField {
  const KeyValueField(this.key, this.value);

  final String key;
  final String value;
}
